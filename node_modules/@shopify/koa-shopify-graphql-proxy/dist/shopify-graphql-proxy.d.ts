/// <reference types="koa" />
import { Context } from 'koa';
export interface SessionContext extends Context {
    session?: {
        accessToken?: string;
        shop?: string;
    };
}
export declare const PROXY_BASE_PATH = "/graphql";
export declare const GRAPHQL_PATH = "/admin/api/graphql";
export default function shopifyGraphQLProxy(ctx: SessionContext, next: () => Promise<any>): Promise<void>;
